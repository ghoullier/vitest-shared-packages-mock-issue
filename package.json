{
  "name": "vitest-shared-packages-mock-issue",
  "private": true,
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "test": "npm run test --workspace=@vitest-shared-packages-mock-issue/app"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ]
}
